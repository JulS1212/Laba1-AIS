<!-- ArtGallery.WPF/GroupedView.xaml -->
<Window x:Class="ArtGallery.WPF.GroupedView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Картины по жанрам" 
        Height="500" 
        Width="700"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <!-- Простой ListView для отображения групп -->
        <ListView ItemsSource="{Binding GenreGroups}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Expander Header="{Binding GenreName}" IsExpanded="True">
                        <ListView ItemsSource="{Binding Paintings}" 
                                  BorderThickness="0" 
                                  Background="Transparent">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="5">
                                        <TextBlock Text="{Binding Title}" 
                                                   FontWeight="Bold"/>
                                        <TextBlock Text="{Binding Artist}" 
                                                   FontStyle="Italic"/>
                                        <TextBlock Text="{Binding Year}" 
                                                   Foreground="Gray"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Expander>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>